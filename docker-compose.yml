services:
  fb-marketplace-watcher:
    build: .
    container_name: fb-marketplace-watcher
    restart: unless-stopped
    volumes:
      - /srv/docker/fb-marketplace-watcher/config.yml:/app/config.yml:ro
      - /srv/docker/fb-marketplace-watcher/data:/app/data
